add_path_config(
  'ruby-source-path',
  '',
  'path to the ruby source code')

add_path_config(
  'ruby-include-path',
  '',
  'path to the ruby header files')

add_bool_config(
  'without-tests',
  false,
  'does not run tests')
